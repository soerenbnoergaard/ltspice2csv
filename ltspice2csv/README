How to use:
===========

With DC (time, voltage):

  ltspice2csv -i INPUT.txt -o OUTPUT.csv

With AC (freq, (mag, phase)):

  ltspice2csv -i INPUT.txt -o OUTPUT.csv -a

INSTALL:
========

Compile:

  make

Install:

  make install

Uninstall

  make uninstall

PLOT DATA IN MATLAB
===================

 %% Read and plot csv
 clc
 m = csvread('dc.csv', 1, 0);
 freq = m(:,1);
 mag = m(:,2);
 phase = m(:,3);
 plot (freq,mag,freq,phase)  
